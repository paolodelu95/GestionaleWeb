{
  "name": "gestionaleweb",
  "version": "1.0.0",
  "description": "Gestionale magazzino web — Angular 21 + Node.js + SQLite.",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"backend,frontend\" -c \"cyan.bold,green.bold\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/paolodelu95/Ordeva.git"
  },
  "license": "ISC"
}
