export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  /** Edizione offline desktop/Electron: niente login/SaaS, email via client di sistema. */
  offline: false,
};
